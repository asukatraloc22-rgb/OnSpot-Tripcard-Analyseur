/* TripCard ELITE — Liquid Glass OnSpot. Chaque contrôle est une décision traçable : statut, constat, preuve et action. */
import { useMemo, useRef, useState } from "react";
import { AlertTriangle, Archive, ArrowDownToLine, Check, ChevronRight, ClipboardPaste, FileJson, Flag, Gauge, Inbox, MapPin, Paperclip, Plane, RotateCcw, Search, ShieldCheck, Sparkles, Upload, Users, X } from "lucide-react";
import { toast } from "sonner";
import { analyzeTrip, demoPayload, normalizeReport, type AuditCheck, type AuditIssue, type AuditReminder, type AuditReport, type AuditStatus, type DocumentCheck, type TripStep } from "@/lib/audit";

const markUrl = "/onspot-favicon.svg";
type Tab = "overview" | "checks" | "itinerary" | "actions" | "documents";

const statusLabel = (status: AuditStatus) => status === "ok" ? "Conforme" : status === "critical" ? "Bloquant" : "À vérifier";
const formatDate = (value: string) => {
  if (!value || value === "—") return "—";
  const parsed = new Date(`${value}T12:00:00`);
  return Number.isNaN(parsed.getTime()) ? value : new Intl.DateTimeFormat("fr-FR", { day: "2-digit", month: "short", year: "numeric" }).format(parsed).replace(".", "");
};

function StatusPill({ status }: { status: AuditStatus }) {
  const className = status === "ok" ? "status-pill status-ok" : status === "critical" ? "status-pill status-critical" : status === "warning" ? "status-pill status-warning" : "status-pill status-pending";
  return <span className={className}><span className="status-dot" />{statusLabel(status)}</span>;
}

function StepIcon({ type }: { type: string }) {
  if (type === "Vol") return <Plane size={17} />;
  if (type === "Hôtel") return <Archive size={17} />;
  if (["Transfert", "Train", "Ferry", "Location voiture"].includes(type)) return <ArrowDownToLine size={17} />;
  return <MapPin size={17} />;
}

function IssueCard({ issue, resolved, onResolve }: { issue: AuditIssue; resolved: boolean; onResolve: () => void }) {
  const className = resolved ? "issue-card issue-resolved" : issue.severity === "critical" ? "issue-card issue-critical" : "issue-card issue-warning";
  return <article className={className}>
    <div className="issue-icon">{resolved ? <Check size={17} /> : issue.severity === "critical" ? <X size={17} /> : <AlertTriangle size={17} />}</div>
    <div className="issue-copy"><div className="issue-kicker">{resolved ? "Traité par l’agent" : issue.severity === "critical" ? "Action prioritaire" : "Point à vérifier"}</div><h3>{issue.title}</h3><p>{issue.detail}</p>{issue.source && <div className="issue-source">Preuve : {issue.source}</div>}{issue.action && <div className="issue-action"><Flag size={13} />{issue.action}</div>}</div>
    <button className="resolve-button" onClick={onResolve}>{resolved ? "Rouvrir" : "Traiter"}</button>
  </article>;
}

function CheckCard({ item }: { item: AuditCheck }) {
  const className = item.status === "ok" ? "check-card check-ok" : item.status === "critical" ? "check-card check-critical" : item.status === "warning" ? "check-card check-warning" : "check-card check-pending";
  return <article className={className}>
    <div className="check-card-head"><div><span className="check-domain">{item.domain}</span><h3>{item.label}</h3></div><StatusPill status={item.status} /></div>
    <div className="check-detail"><span>Constat</span><p>{item.finding}</p></div>
    <div className="check-evidence"><Paperclip size={14} /><div><span>Preuve lue dans l’export</span><p>{item.evidence}</p></div></div>
    {item.action && <div className="check-action"><Flag size={13} /><div><span>Action agent</span><p>{item.action}</p></div></div>}
  </article>;
}

function ReminderList({ reminders }: { reminders: AuditReminder[] }) {
  const label = (kind: AuditReminder["kind"]) => kind === "CHECK-IN" ? "Check-in vol" : kind === "WELCOME" ? "Welcome call" : "Activité / transfert · H-24";
  const rowClass = (kind: AuditReminder["kind"]) => kind === "CHECK-IN" ? "reminder-row reminder-check-in" : kind === "WELCOME" ? "reminder-row reminder-welcome" : "reminder-row reminder-h-24";
  const checkins = reminders.filter((reminder) => reminder.kind === "CHECK-IN").length;
  const welcomes = reminders.filter((reminder) => reminder.kind === "WELCOME").length;
  const reconfirmations = reminders.filter((reminder) => reminder.kind === "H-24").length;
  return <section className="reminder-panel"><div className="panel-heading"><div><p className="eyebrow">Agenda opérationnel</p><h2>Rappels à faire</h2></div><span className="mono">HEURES · UTC</span></div><div className="reminder-summary"><div><b>{checkins}</b><span>check-in vols<br />à H-24</span></div><div><b>{welcomes}</b><span>welcome calls<br />à H+5</span></div><div><b>{reconfirmations}</b><span>reconfirmations<br />uniquement selon voucher</span></div></div>{reminders.length ? <div className="reminder-list">{reminders.map((reminder) => <div className={rowClass(reminder.kind)} key={reminder.id}><div className="reminder-date"><b>{formatDate(reminder.date)}</b><small>{reminder.time ? reminder.time + " UTC" : "Heure à confirmer"}</small></div><div className="reminder-main"><span className="reminder-kind">{label(reminder.kind)}</span><strong>{reminder.label.replace(/^(Check-in|Welcome call|Reconfirmation)\s*·\s*/, "")}</strong><p>{reminder.detail}</p></div><span className="reminder-status">À faire</span></div>)}</div> : <div className="clear-state"><Check size={20} /><div><b>Aucun rappel calculable pour le moment.</b><p>Un welcome call n’est jamais calculé sans heure d’arrivée et une reconfirmation ne l’est que si le voucher l’exige.</p></div></div>}</section>;
}

function DocumentStatus({ status }: { status: DocumentCheck["status"] }) {
  const labels = { present: "Présent", missing: "Manquant", pending: "À vérifier", "not-applicable": "Non applicable" };
  const className = status === "present" ? "document-status document-present" : status === "missing" ? "document-status document-missing" : status === "pending" ? "document-status document-pending" : "document-status document-not-applicable";
  return <span className={className}><span />{labels[status]}</span>;
}

function StepRow({ step }: { step: TripStep }) {
  return <div className="step-row"><div className="step-rail"><div className="step-icon"><StepIcon type={step.type} /></div><div className="step-line" /></div><div className="step-content"><div className="step-meta"><span>{step.type}</span><span className="mono">{step.date && step.date !== "—" ? formatDate(step.date) : "Date à confirmer"}{step.time ? ` · ${step.time}` : ""}</span></div><h3>{step.title}</h3><p><MapPin size={13} />{step.location}</p><small>{step.detail}</small></div><StatusPill status={step.status} /></div>;
}

function EmptyImport({ onDemo, onPaste, inputRef }: { onDemo: () => void; onPaste: () => void; inputRef: React.RefObject<HTMLInputElement | null> }) {
  return <div className="import-state"><div className="import-intro"><div className="import-symbol"><Inbox size={28} /></div><p className="eyebrow">Nouveau contrôle · aucun dossier chargé</p><h1>Vérifier un dossier,<br /><em>avant le départ.</em></h1><p className="lead">Importez le JSON de l’extension OnSpot Audit Assistant. TripCard ELITE contrôle les éléments structurés localement et affiche toujours la preuve utilisée.</p><div className="import-actions"><button className="button button-primary" onClick={() => inputRef.current?.click()}><Upload size={17} />Importer un fichier JSON</button><button className="button button-secondary" onClick={onPaste}><ClipboardPaste size={17} />Coller depuis le presse-papiers</button></div><button className="demo-link" onClick={onDemo}><Sparkles size={14} />Ouvrir le dossier de démonstration</button></div><div className="audit-frame"><div className="audit-frame-head"><span className="mono">TRAME DE CONTRÔLE · PREUVES V2</span><span className="frame-status"><span />PRÊT À IMPORTER</span></div><div className="route-motif"><div className="route-start" /><div className="route-line" /><div className="route-stop"><MapPin size={13} /></div></div><div className="frame-grid"><div><span className="frame-number">01</span><b>Dossier & voyageurs</b><small>Identité · dates · notes</small></div><div><span className="frame-number">02</span><b>Segments & vouchers</b><small>Vols · hôtels · transports</small></div><div><span className="frame-number">03</span><b>Cohérence prouvée</b><small>Horaires · correspondances</small></div><div><span className="frame-number">04</span><b>Actions UTC</b><small>Check-in · welcome call · H-24</small></div></div><div className="frame-footer"><span className="mono">SOURCE</span><b>JSON EXTENSION ONSPOT</b><span className="ticket-perf" /></div></div><div className="import-side-note"><div className="note-rule" /><span className="mono">MODE LOCAL · GRATUIT</span><p>Le dossier reste dans le navigateur. Aucun statut « conforme » n’est affiché sans élément de preuve.</p></div></div>;
}

export default function Home() {
  const [report, setReport] = useState<AuditReport | null>(() => { try { if (new URLSearchParams(window.location.search).has("demo")) return analyzeTrip(demoPayload); const saved = localStorage.getItem("tripcard:last-report"); const normalized = saved ? normalizeReport(JSON.parse(saved)) : null; if (saved && !normalized) localStorage.removeItem("tripcard:last-report"); return normalized; } catch { localStorage.removeItem("tripcard:last-report"); return null; } });
  const [tab, setTab] = useState<Tab>("overview");
  const [resolved, setResolved] = useState<string[]>([]);
  const [showRaw, setShowRaw] = useState(false);
  const [searchTerm, setSearchTerm] = useState("");
  const inputRef = useRef<HTMLInputElement>(null);
  const loadPayload = (payload: Record<string, unknown>, source: string) => { const next = analyzeTrip(payload); setReport(next); setTab("overview"); setResolved([]); localStorage.setItem("tripcard:last-report", JSON.stringify(next)); toast.success(`Dossier importé · ${source}`, { description: `${next.stats.critical} bloquant(s), ${next.stats.warnings} point(s) à vérifier.` }); };
  const handleFile = (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => { try { loadPayload(JSON.parse(String(reader.result)), file.name); } catch { toast.error("JSON illisible", { description: "Le fichier ne semble pas être un JSON valide." }); } }; reader.readAsText(file); event.target.value = ""; };
  const handlePaste = async () => { try { const content = await navigator.clipboard.readText(); if (!content) throw new Error("empty"); loadPayload(JSON.parse(content), "presse-papiers"); } catch { toast.error("Impossible de lire le presse-papiers", { description: "Autorisez l’accès ou utilisez l’import de fichier JSON." }); } };
  const filteredSteps = useMemo(() => report?.steps.filter((step) => `${step.title} ${step.location} ${step.type}`.toLowerCase().includes(searchTerm.toLowerCase())) ?? [], [report, searchTerm]);
  const unresolved = report?.issues.filter((issue) => !resolved.includes(issue.id)) ?? [];
  const score = report ? Math.round((report.stats.passed / Math.max(report.stats.checked, 1)) * 100) : 0;
  const openChecks = report?.checks.filter((item) => item.status !== "ok").length ?? 0;

  return <div className="app-shell"><aside className="sidebar"><div className="brand"><img className="brand-logo" src={markUrl} alt="OnSpot" /><div><span className="brand-name">TripCard</span><span className="brand-tier">ELITE</span></div></div><div className="sidebar-caption">ONSPOT TRAVEL SOLUTIONS<br /><span>POSTE DE CONTRÔLE · CARSON CITY</span></div><nav className="side-nav"><button className="side-nav-item active"><Gauge size={17} />Contrôle en cours</button><button className="side-nav-item" onClick={() => toast.info("La liste des dossiers sera ajoutée dans une prochaine étape.")}><Archive size={17} />Dossiers récents<span className="nav-count">V2</span></button><button className="side-nav-item" onClick={() => report ? setTab("checks") : toast.info("Importez un dossier pour consulter les règles appliquées.")}><ShieldCheck size={17} />Règles de contrôle</button></nav><div className="sidebar-footer"><div className="connection-mark"><span />Mode local actif</div><p>Aucune donnée n’est envoyée sans votre action explicite.</p><div className="agent-chip"><span className="avatar">P</span><span><b>Patrick</b><small>Agent ELITE</small></span></div></div></aside><main className="main-area"><header className="topbar"><div><span className="top-kicker">AUDIT DE VOYAGE</span><span className="top-divider">/</span><span className="mono">TRIPCARD ELITE</span></div><div className="top-actions"><span className="local-badge"><span />Local · gratuit</span><button className="icon-button" onClick={() => report ? setShowRaw(!showRaw) : toast.info("Importez d’abord un dossier.")} aria-label="Afficher le JSON"><FileJson size={17} /></button><button className="button button-primary compact" onClick={() => inputRef.current?.click()}><Upload size={15} />Importer</button></div></header><input ref={inputRef} type="file" accept="application/json,.json" hidden onChange={handleFile} />{!report ? <EmptyImport onDemo={() => loadPayload(demoPayload, "démo locale")} onPaste={handlePaste} inputRef={inputRef} /> : <><section className="trip-header"><div className="trip-header-top"><div><span className="eyebrow">Dossier contrôlé · {report.reference}</span><h1>{report.tripName}</h1><p className="destination"><MapPin size={15} />{report.destination}</p></div><div className="score-stamp"><div className="score-number">{score}<small>%</small></div><div><span>Lecture locale</span><b>{report.stats.critical ? "Action requise" : openChecks ? "À vérifier" : "Conforme"}</b></div></div></div><div className="trip-facts"><div><span className="fact-label">Période</span><b>{formatDate(report.startDate)} <i>→</i> {formatDate(report.endDate)}</b></div><div><span className="fact-label">Voyageurs</span><b><Users size={14} />{report.travelers.length ? report.travelers.join(" · ") : "À identifier"}</b></div><div><span className="fact-label">Contrôles</span><b><span className="mini-status" />{report.stats.checked} règles exécutées</b></div><button className="reset-button" onClick={() => { setReport(null); localStorage.removeItem("tripcard:last-report"); }}><RotateCcw size={14} />Nouveau dossier</button></div></section><div className="content-layout"><section className="content-column"><div className="tabs" role="tablist"><button className={tab === "overview" ? "tab active" : "tab"} onClick={() => setTab("overview")}>Synthèse <span>{openChecks}</span></button><button className={tab === "checks" ? "tab active" : "tab"} onClick={() => setTab("checks")}>Contrôles <span>{report.checks.length}</span></button><button className={tab === "itinerary" ? "tab active" : "tab"} onClick={() => setTab("itinerary")}>Itinéraire <span>{report.steps.length}</span></button><button className={tab === "documents" ? "tab active" : "tab"} onClick={() => setTab("documents")}>Documents <span>{report.documentChecks.length}</span></button><button className={tab === "actions" ? "tab active" : "tab"} onClick={() => setTab("actions")}>Actions <span>{unresolved.length || "·"}</span></button></div>{tab === "overview" && <div className="overview-stack"><div className="section-intro"><div><p className="eyebrow">Lecture opérationnelle</p><h2>Un constat, une preuve,<br /><em>une décision agent.</em></h2></div><span className="mono">MOTEUR LOCAL · RÈGLES V2</span></div><div className="domain-grid">{report.domains.map((domain) => <button key={domain.label} className="domain-card" onClick={() => setTab(domain.label === "Cohérence" || domain.label === "Dossier" || domain.label === "Voyageurs" ? "checks" : domain.label === "Hébergements" || domain.label === "Transports" || domain.label === "Activités" || domain.label === "Documents" ? "documents" : domain.label === "Rappels" ? "overview" : "itinerary")}><div className="domain-top"><StatusPill status={domain.status} /><ChevronRight size={16} /></div><h3>{domain.label}</h3><p>{domain.note}</p><span className="domain-count">{domain.count} <small>contrôles</small></span></button>)}</div><div className="control-strip"><div><span className="fact-label">Métadonnées</span><b>{report.metadata.agency}</b><small>{report.reference} · {report.metadata.package}</small></div><div><span className="fact-label">Identité jointe</span><b className={report.metadata.identityDocuments.length ? "" : "amber-text"}>{report.metadata.identityDocuments.length ? "Fichier(s) trouvé(s)" : "Non prouvée"}</b><small>{report.metadata.identityDocuments.length ? report.metadata.identityDocuments.join(" · ") : "Mention textuelle ignorée"}</small></div><div><span className="fact-label">Profil client</span><b>{report.metadata.profileNotes.length ? "Attention signalée" : "À consulter"}</b><small>{report.metadata.profileNotes[0] || "Aucune note exportée"}</small></div><div><span className="fact-label">Actions</span><b>{unresolved.length} à traiter</b><small>Contrôles ouverts et rappels UTC</small></div></div><ReminderList reminders={report.reminders} /><div className="attention-heading"><div><p className="eyebrow">Actions finales</p><h2>{unresolved.length ? `${unresolved.length} point${unresolved.length > 1 ? "s" : ""} à clarifier` : "Aucun point ouvert"}</h2></div><button className="text-button" onClick={() => setTab(unresolved.length ? "actions" : "checks")}>{unresolved.length ? "Voir les actions" : "Voir les contrôles"}<ChevronRight size={15} /></button></div>{unresolved.slice(0, 3).map((issue) => <IssueCard key={issue.id} issue={issue} resolved={false} onResolve={() => setResolved((items) => [...items, issue.id])} />)}{unresolved.length === 0 && <div className="clear-state"><ShieldCheck size={22} /><div><b>Aucun point ouvert dans le contrôle local.</b><p>{report.stats.passed} règles sont conformes dans les données disponibles ; les contrôles non applicables ou non exportés restent explicitement signalés.</p></div></div>}</div>}{tab === "checks" && <div className="tab-panel"><div className="panel-heading"><div><p className="eyebrow">Référentiel exécuté</p><h2>Contrôles, preuves & décisions</h2></div><span className="mono">{report.checks.length} RÈGLES · {openChecks} À VÉRIFIER</span></div><p className="checks-intro">Chaque ligne indique ce qui a été contrôlé, l’élément lu dans l’export et l’action qui reste éventuellement à mener. Le système ne déduit pas un document joint d’une simple mention textuelle.</p><div className="check-list">{report.checks.map((item) => <CheckCard key={item.id} item={item} />)}</div></div>}{tab === "itinerary" && <div className="tab-panel"><div className="panel-heading"><div><p className="eyebrow">Fil du voyage</p><h2>Étapes détectées</h2></div><div className="search-field"><Search size={15} /><input value={searchTerm} onChange={(event) => setSearchTerm(event.target.value)} placeholder="Filtrer les étapes" /></div></div><div className="timeline">{filteredSteps.length ? filteredSteps.map((step) => <StepRow key={step.id} step={step} />) : <p className="empty-line">Aucune étape ne correspond à cette recherche.</p>}</div></div>}{tab === "documents" && <div className="tab-panel"><div className="panel-heading"><div><p className="eyebrow">Pièces jointes du dossier</p><h2>Documents & vouchers</h2></div><span className="mono">LECTURE STRUCTURELLE</span></div><div className="document-panel"><div className="document-visual"><Paperclip size={24} /><span>VOUCHERS</span></div><div><h3>Contrôle des pièces du dossier</h3><p>Un fichier n’est accepté que s’il contient les marqueurs attendus. Les mentions de passeport ou CNI dans un voucher hôtel ne constituent jamais une preuve de pièce d’identité jointe.</p><div className="document-check-list">{report.documentChecks.map((item) => <div className="document-check-row" key={item.id}><div><b>{item.label}</b><small>{item.evidence}</small></div><DocumentStatus status={item.status} /></div>)}</div><div className="document-meta"><span><FileJson size={14} />{report.documentChecks.length} familles contrôlées</span><span><Check size={14} />Preuves locales</span></div></div></div></div>}{tab === "actions" && <div className="tab-panel"><div className="panel-heading"><div><p className="eyebrow">Registre des décisions</p><h2>Actions à traiter</h2></div><span className="mono">{report.issues.length} SIGNALÉES · {resolved.length} TRAITÉES</span></div><div className="issues-list">{report.issues.length ? report.issues.map((issue) => <IssueCard key={issue.id} issue={issue} resolved={resolved.includes(issue.id)} onResolve={() => setResolved((items) => items.includes(issue.id) ? items.filter((id) => id !== issue.id) : [...items, issue.id])} />) : <div className="clear-state"><ShieldCheck size={22} /><div><b>Aucune action déclenchée par les règles locales.</b><p>Consultez l’onglet Contrôles pour lire les vérifications conformes et non applicables.</p></div></div>}</div></div>}</section><aside className="right-rail"><div className="rail-card"><div className="rail-card-head"><span className="eyebrow">État du contrôle</span><StatusPill status={report.stats.critical ? "critical" : openChecks ? "warning" : "ok"} /></div><div className="progress-ring" style={{ "--score": score } as React.CSSProperties}><div><b>{score}</b><span>/ 100</span></div></div><p className="rail-note">Indicateur de complétude des preuves exportées. Il ne remplace pas le jugement de l’agent et n’efface aucune vérification métier.</p><div className="rail-stat"><span>Conformes</span><b>{report.stats.passed}</b></div><div className="rail-stat"><span>À vérifier</span><b className="amber-text">{report.stats.warnings}</b></div><div className="rail-stat"><span>Bloquants</span><b className="red-text">{report.stats.critical}</b></div></div><div className="rail-card next-action"><p className="eyebrow">Prochaine action</p><h3>{report.stats.critical ? "Traiter les blocages avant la suite." : openChecks ? "Documenter les éléments restants." : "Poursuivre avec les rappels UTC."}</h3><p>Le contrôle met en avant les preuves manquantes ; les confirmations partenaires restent à réaliser dans les canaux habituels.</p><button className="button button-secondary full" onClick={() => setTab(unresolved.length ? "actions" : "checks")}>{unresolved.length ? "Ouvrir les actions" : "Voir les contrôles"}<ChevronRight size={15} /></button></div><div className="rail-quote"><span>“</span><p>Une vérification utile explique toujours ce qu’elle sait — et ce qu’elle ignore.</p><small>PHILOSOPHIE ELITE · ONSPOT</small></div></aside></div></>}</main>{showRaw && report && <div className="raw-overlay" onClick={() => setShowRaw(false)}><div className="raw-dialog" onClick={(event) => event.stopPropagation()}><div className="raw-head"><div><p className="eyebrow">Source importée</p><h2>JSON du dossier</h2></div><button className="icon-button" onClick={() => setShowRaw(false)}><X size={17} /></button></div><pre>{JSON.stringify(report.raw, null, 2)}</pre></div></div>}</div>;
}
